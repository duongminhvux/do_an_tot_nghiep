"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
import { Logo } from "@/components/ui/logo";
import { Button } from "@/components/ui/button";
const links = [{ label: "Courses", href: "#courses" }, { label: "TOEIC", href: "#toeic" }, { label: "How it works", href: "#how-it-works" }, { label: "Pricing", href: "#pricing" }, { label: "Blog", href: "#blog" }];
export function PublicHeader() { const [open, setOpen] = useState(false); return <header className="sticky top-0 z-40 border-b border-slate-200/80 bg-white/95 backdrop-blur"><div className="mx-auto flex h-18 max-w-7xl items-center justify-between px-4 md:px-6"><Logo /><nav className="hidden items-center gap-7 lg:flex">{links.map((link) => <Link key={link.label} className="text-sm font-medium text-slate-600 hover:text-blue-700" href={link.href}>{link.label}</Link>)}</nav><div className="hidden items-center gap-2 md:flex"><Link href="/login" className="px-3 py-2 text-sm font-semibold text-slate-600">Log in</Link><Link href="/register"><Button>Start Learning</Button></Link></div><button className="grid size-10 place-items-center rounded-lg lg:hidden" aria-label="Toggle navigation" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></div>{open && <nav className="border-t border-slate-100 bg-white p-4 lg:hidden">{links.map((link) => <Link onClick={() => setOpen(false)} key={link.label} className="block rounded-lg px-4 py-3 font-medium text-slate-700 hover:bg-blue-50" href={link.href}>{link.label}</Link>)}<div className="mt-3 grid grid-cols-2 gap-2"><Link href="/login"><Button variant="secondary" className="w-full">Log in</Button></Link><Link href="/register"><Button className="w-full">Start Learning</Button></Link></div></nav>}</header>; }
