import Link from "next/link";
import { BookOpen, Users } from "lucide-react";
import type { CourseCardDto } from "@/domain/dto";
import { CourseVisual } from "./course-visual";
import { ProgressBar } from "../ui/progress-bar";
export function CourseCard({ course, publicView = false }: { course: CourseCardDto; publicView?: boolean }) { return <Link href={publicView ? `/login?next=/app/courses/${course.slug}` : `/app/courses/${course.slug}`} className="surface card-hover block overflow-hidden"><CourseVisual theme={course.theme} className="min-h-40 rounded-none" label={course.title} /><div className="p-4"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{course.level}</span><h3 className="mt-3 text-lg font-bold tracking-tight">{course.title}</h3><div className="mt-1 flex items-center gap-4 text-xs text-slate-500"><span className="flex items-center gap-1"><BookOpen className="size-3.5" />{course.lessonCount} lessons</span><span className="flex items-center gap-1"><Users className="size-3.5" />{course.learnerCount.toLocaleString()}</span></div><p className="mt-3 line-clamp-2 text-sm text-slate-600">{course.description}</p><div className="mt-4"><ProgressBar value={course.progress || Math.min(88, course.learnerCount / 15)} /><div className="mt-1 text-right text-xs font-semibold text-slate-500">{publicView ? "Popular course" : `${course.progress}% complete`}</div></div></div></Link>; }
