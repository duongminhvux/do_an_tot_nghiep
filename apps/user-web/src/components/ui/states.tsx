import { AlertTriangle, BookOpen, LoaderCircle } from "lucide-react";
import { Button } from "./button";
export function LoadingSkeleton({ rows = 3 }: { rows?: number }) { return <div className="animate-pulse space-y-4" aria-label="Loading"><div className="h-7 w-1/3 rounded bg-slate-200" />{Array.from({ length: rows }).map((_, i) => <div key={i} className="h-28 rounded-xl border border-slate-100 bg-white" />)}</div>; }
export function EmptyState({ title = "Nothing here yet", description = "New learning activity will appear here." }: { title?: string; description?: string }) { return <div className="surface grid min-h-56 place-items-center p-8 text-center"><div><BookOpen className="mx-auto mb-3 size-9 text-blue-500" /><h3 className="font-bold">{title}</h3><p className="mt-1 text-sm text-slate-500">{description}</p></div></div>; }
export function ErrorState({ message, onRetry }: { message: string; onRetry?: () => void }) { return <div className="surface grid min-h-56 place-items-center p-8 text-center"><div><AlertTriangle className="mx-auto mb-3 size-9 text-red-500" /><h3 className="font-bold">We couldn’t load this</h3><p className="mt-1 text-sm text-slate-500">{message}</p>{onRetry && <Button className="mt-4" onClick={onRetry}>Try again</Button>}</div></div>; }
export function InlineLoader() { return <LoaderCircle className="size-4 animate-spin" aria-hidden />; }
