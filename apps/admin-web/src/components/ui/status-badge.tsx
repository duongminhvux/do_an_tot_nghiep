import { AlertTriangle,CheckCircle2,Clock3,XCircle } from "lucide-react";
import { cn } from "@/lib/utils";
export function StatusBadge({status}:{status:string}){const s=status.toUpperCase();const good=["ACTIVE","PUBLISHED","PASS","COMPLETED","SUCCESS"].includes(s);const bad=["BLOCKED","FAIL","FAILED","ARCHIVED","CANCELLED"].includes(s);const pending=["INVITED","DRAFT","PENDING","PROCESSING"].includes(s);const Icon=good?CheckCircle2:bad?XCircle:pending?Clock3:AlertTriangle;return <span className={cn("inline-flex items-center gap-1 rounded-md px-2 py-1 text-xs font-bold",good&&"bg-green-50 text-green-700",bad&&"bg-red-50 text-red-700",pending&&"bg-amber-50 text-amber-700",!good&&!bad&&!pending&&"bg-blue-50 text-blue-700")}><Icon className="size-3"/>{s.replaceAll("_"," ")}</span>}
