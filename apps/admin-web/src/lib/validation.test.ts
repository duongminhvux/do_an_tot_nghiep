import { describe,expect,it } from "vitest";
import { ExerciseType,TtsJobStatus } from "@listenup/domain";
import { nextTtsStatus,serializeFilters,validateExerciseForPublish } from "./validation";
describe("admin content rules",()=>{it("validates Dictation publishing",()=>{const result=validateExerciseForPublish({type:ExerciseType.DICTATION,title:"Dictation",courseId:"c",lessonId:"l",script:"Hello",audioReady:true,correctText:"Hello",passThreshold:80,maxListens:3,maxAttempts:3});expect(result.valid).toBe(true)});it("rejects incomplete TOEIC configuration",()=>{const result=validateExerciseForPublish({type:ExerciseType.TOEIC,title:"TOEIC",courseId:"c",lessonId:"l",script:"Talk",audioReady:true,toeicPart:null,questions:[],passThreshold:80,maxListens:3,maxAttempts:3});expect(result.errors).toContain("TOEIC_PART_REQUIRED");expect(result.errors).toContain("QUESTION_REQUIRED")});it("applies deterministic TTS transitions",()=>{expect(nextTtsStatus(TtsJobStatus.PENDING,"process")).toBe(TtsJobStatus.PROCESSING);expect(nextTtsStatus(TtsJobStatus.PROCESSING,"fail")).toBe(TtsJobStatus.FAILED);expect(nextTtsStatus(TtsJobStatus.FAILED,"retry")).toBe(TtsJobStatus.PENDING)});it("serializes filters deterministically",()=>expect(serializeFilters({status:"PUBLISHED",page:2,level:"BEGINNER"})).toBe("level=BEGINNER&page=2&status=PUBLISHED"))});
